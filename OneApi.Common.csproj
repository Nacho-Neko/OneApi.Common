<Project>
  <PropertyGroup>
    <ImportDirectoryBuildProps>false</ImportDirectoryBuildProps>
    <ImportDirectoryBuildTargets>false</ImportDirectoryBuildTargets>
  </PropertyGroup>

  <Sdk Name="Microsoft.NET.Sdk" />

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <Description>OneApi cross-service shared primitives: LLM invoke routing, usage kinds/units/billing types, dispatch wire DTOs/error codes, Edge ↔ Gateway MagicOnion dispatch RPC (ILlmDispatchService), catalog slug parsing, local Claude token counting. Single source of truth across OneApi.Gateway, Meeko.Demux and Tavern.Gateway.</Description>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Reverse-engineered Claude 3+/4.x vocabulary (ctoc, 38,360 API-verified tokens),
         gzipped JSON string array. Consumed by ClaudeTokenCounter. -->
    <EmbeddedResource Include="Tokenization\ClaudeVocab.json.gz" LogicalName="OneApi.Common.Tokenization.ClaudeVocab.json.gz" />
  </ItemGroup>

  <ItemGroup Condition="'$(ManagePackageVersionsCentrally)' == 'true'">
    <PackageReference Include="Consul" />
    <PackageReference Include="MagicOnion.Abstractions" />
    <PackageReference Include="MessagePack" />
  </ItemGroup>

  <ItemGroup Condition="'$(ManagePackageVersionsCentrally)' != 'true'">
    <PackageReference Include="Consul" Version="1.8.0" />
    <PackageReference Include="MagicOnion.Abstractions" Version="7.10.0" />
    <PackageReference Include="MessagePack" Version="3.1.7" />
  </ItemGroup>

</Project>
